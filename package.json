{
  "name": "bookmarklet",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "pnpm esbuild --minify --bundle --format=esm --outdir=cmd/ ./src/**/*.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "esbuild": "^0.27.0",
    "typescript": "5.9.3"
  }
}
