# bookmarklets

## how to use

```bash
pnpm install
# build `./src/cmd/*.ts` to `./cmd/*.js` use esbuild
pnpm run build

cat ./cmd/md_copy_link.js | pbcopy
```

add bookmark below.

```js
javascript:<paste>
```